policy:
  allow_implicit_invocation: false
interface:
  display_name: "CMS Cultivator: PR Create"
  short_description: "Generate PR description and create a GitHub pull request with user confirmation before submission."
