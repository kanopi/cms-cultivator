policy:
  allow_implicit_invocation: false
interface:
  display_name: "CMS Cultivator: PR Release"
  short_description: "Generate changelog, deployment checklist, and update PR for release with user confirmation before changes."
